export const metadata = {
    title: 'Job Sites Directory',
    description:
        'Browse trusted job platforms, pin your go-to sites, and streamline your daily application workflow.',
    alternates: {
        canonical: '/job-sites',
    },
};

export default function JobSitesLayout({ children }) {
    return children;
}
