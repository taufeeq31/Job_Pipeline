export const metadata = {
    title: 'Resume Manager',
    description:
        'Create and compare resume versions, monitor performance, and refine your applications with resume insights.',
    alternates: {
        canonical: '/resumes',
    },
};

export default function ResumesLayout({ children }) {
    return children;
}
